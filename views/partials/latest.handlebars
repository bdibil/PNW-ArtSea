{{#if latest}}
    <div class="card-group">
        {{#each latest}}
            <div class="card">
                <img class="card-img-top" src="{{this.cloud_url}}" alt="">
                <div class="card-body">
                  <p class="card-text">{{this.artist_name}}</p>
                </div>
            </div>
        {{/each}}
    </div>
{{else}}
    <p><em>There are currently no arts.</em></p>
{{/if}}

{{!-- {{#if latest}}
<div class="container-fluid d-flex justify-content-center mt-20">
    <div class="row">
        <div class="col-sm-4">
             {{#each latest}}
            <div class="card">
                <div class="card-body text-center"> <img src="{{this.cloud_url}}" alt="" class="rounded-circle" width="100" height="100">
                    <p class="mb-3 mt-3">{{this.artist_name}}</p>
            </div>
            {{/each}}
        </div>
    </div>
</div>
{{else}}
    <p><em>There are currently no arts.</em></p>
{{/if}} --}}